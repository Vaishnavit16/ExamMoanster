@* @model IEnumerable<MockTestQuestion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-success">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                MockTestQuestionID
            </th>
            <th>
                MockTestSubjectID
            </th>
            <th>
                Question
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var temp in Model)
        {
            <tr>
                <td>
                    @temp.MockTestQuestionID
                </td>
                <td>
                    @temp.MockTestSubjectID
                </td>
                <td>
                    @temp.Questions
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@temp.MockTestQuestionID" class="btn btn-primary">Edit</a>

                </td>
                <td>
                    <a asp-action="Delete" asp-route-id="@temp.MockTestQuestionID" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
 *@