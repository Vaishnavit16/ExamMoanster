@* @* @model ManageQuestionsViewModel

<h4>Questions in @Model.Questions.FirstOrDefault()?.QuestionBank?.QuestionBankTitle</h4>

@if (TempData["Message"] != null)
{
    <div class="alert alert-warning">
        @TempData["Message"]
    </div>
}

<div>
    <a asp-action="Create" asp-controller="Company" asp-route-questionBankId="@Model.QuestionBankId" class="btn btn-primary"> Create </a>

</div>
<br /><br />

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Question Title</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var temp in Model.Questions)
        {
            <tr>
                <td>@temp.QuestionTitle</td>
                <td>
                    <a asp-action="edit" asp-route-id="@temp.QuestionID"> Edit </a>
                    <a asp-action="delete" asp-route-id="@temp.QuestionID"> Delete </a>

                </td>
            </tr>
        }
    </tbody>
</table>
 *@






@*  @model ManageQuestionsViewModel

<h4>Questions in @Model.Questions.FirstOrDefault()?.QuestionBank?.QuestionBankTitle</h4>

@if (TempData["Message"] != null)
{
    <div class="alert alert-warning">
        @TempData["Message"]
    </div>
}

<div>
    <a asp-action="Create" asp-controller="Company" asp-route-questionBankId="@Model.QuestionBankId" class="btn btn-primary"> Create </a>
</div>
<br />
<br />

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Question Title</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var temp in Model.Questions)
        {
            <tr>
                <td>@temp.QuestionTitle</td>
                <td>
                    

                    <!-- Delete Button -->
                    <form method="post" asp-action="Delete" asp-route-id="@temp.QuestionID" style="display:inline;">
                        <button type="submit" class="btn btn-primaryd" onclick="return confirm('Are you sure you want to delete this question?');"> Delete </button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table> *@








@* @model ManageQuestionsViewModel

<h4>Questions in @Model.Questions.FirstOrDefault()?.QuestionBank?.QuestionBankTitle</h4>

@if (TempData["Message"] != null)
{
    <div class="alert alert-warning">
        @TempData["Message"]
    </div>
}

<div>
    <a asp-action="Create" asp-controller="Company" asp-route-questionBankId="@Model.QuestionBankId" class="btn btn-primary"> Create </a>
</div>
<br />
<br />

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Question Title</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var temp in Model.Questions)
        {
            <tr>
                <td>@temp.QuestionTitle</td>
                <td>
                    <!-- Delete Button -->
                    <form method="post" asp-action="Delete" asp-route-id="@temp.QuestionID" style="display:inline;">
                        <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this question?');"> Delete </button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
 *@ 


 @model ManageQuestionsViewModel

<div class="col-6">
    <div class="card">
        <div class="card-body">

            <h4><b>Questions in @Model.Questions.FirstOrDefault()?.QuestionBank?.QuestionBankTitle</b></h4>

@if (TempData["Message"] != null)
{
    <div class="alert alert-warning">
        @TempData["Message"]
    </div>
}
<br>


<div>
    <a asp-action="Create" asp-controller="Company" asp-route-questionBankId="@Model.QuestionBankId" class="btn btn-primary">Create</a>
</div>
<br />
<br />

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Question Title</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var temp in Model.Questions)
        {
            <tr>
                <td>@temp.QuestionTitle</td>
                <td>
                    <a asp-action="QuestionDetail" asp-route-questionId="@temp.QuestionID" class="btn btn-primary"> Details</a>
                              <a asp-action="Edit" asp-route-id="@temp.QuestionID" class="btn btn-primary"> Edit</a>   

                   
                    <form method="post" asp-action="DeleteQuestion" asp-route-questionId="@temp.QuestionID" asp-route-questionBankId="@Model.QuestionBankId" style="display:inline;">
                        <button type="submit" class="btn btn-primary" onclick="return confirm('Are you sure you want to delete this question?');">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>


            <div>
                <a asp-action="Index" asp-controller="Company" class="btn btn-primary"> Back to QuestionBank List </a>
            </div>
        </div>
</div>
</div>